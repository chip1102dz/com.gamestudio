node {
    stage('SCM') {
        checkout scm
    }
    stage('SonarQube Analysis') {
        withSonarQubeEnv('SonarQube') { // Thay 'SonarQube' bằng tên bạn đã cấu hình trong Jenkins
            sh './gradlew sonar' // Hoặc './gradlew.bat sonar' nếu bạn đang sử dụng Windows
        }
    }
}